# MLB Analytics CLI System — Python Dependencies
# Install with: pip install -r requirements.txt

requests>=2.31.0       # HTTP client for all API calls
rich>=13.7.0           # Modern terminal UI (tables, panels, progress bars)
pandas>=2.0.0          # Optional: data manipulation (available for future extensions)
